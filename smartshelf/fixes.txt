You are a senior software architect and Next.js engineer.

Before making ANY modifications, first analyze the existing SmartShelf project and understand its architecture completely.

The project already has:

- Next.js App Router
- Prisma ORM
- PostgreSQL (Neon)
- JWT authentication using jose
- localStorage authentication
- Middleware protecting routes
- Role system (Super Admin, Admin, Pharmacist)
- Offline support (Service Worker + IndexedDB)
- WhatsApp chatbot
- AI assistant
- Pharmacy management
- User management

IMPORTANT:

Do NOT rewrite the architecture.

Preserve:

✓ JWT implementation
✓ Middleware
✓ Role-based authorization
✓ Protected routes
✓ IndexedDB
✓ Offline functionality
✓ Dashboard
✓ Reports
✓ Inventory
✓ AI Assistant
✓ WhatsApp chatbot
✓ Restock notifications

The ONLY authentication feature to replace is the WhatsApp OTP login system.

------------------------------------------------

REMOVE

Completely remove the dependency on OTP authentication.

Delete or deprecate:

- send-otp flow
- verify-otp flow
- OTP verification pages
- OTP generation logic

The WhatsApp server must NO LONGER be required for login.

It should remain only for:

- AI assistant
- Restock notifications
- Future messaging

------------------------------------------------

DATABASE

Modify the User model.

Add:

passwordHash String

mustChangePassword Boolean @default(true)

failedLoginAttempts Int @default(0)

lockedUntil DateTime?

lastLoginAt DateTime?

passwordChangedAt DateTime?

createdById String?

Add the necessary Prisma relationships without breaking existing relations.

Create a migration.

------------------------------------------------

PASSWORDS

Use bcrypt.

Never store plaintext passwords.

Hash every password before saving.

Use bcrypt.compare() during login.

------------------------------------------------

LOGIN

Replace

Phone -> OTP

with

Phone Number + Password

Workflow:

User enters phone number.

Normalize the phone exactly as the current implementation already does.

Find user.

Compare password using bcrypt.

If invalid:

Increase failedLoginAttempts.

After 5 failed attempts:

Lock account for 15 minutes.

Return appropriate error.

If successful:

Reset failedLoginAttempts.

Update lastLoginAt.

Issue the existing JWT exactly as before.

Reuse the current JWT implementation.

Store JWT exactly as before.

Do not rewrite the authentication architecture.

------------------------------------------------

USER CREATION

Super Admin creates Admin.

Admin creates Pharmacists.

When creating a user:

Generate a secure temporary password automatically.

Example format:

RX-8G4K-29

Hash it.

Store passwordHash.

Set

mustChangePassword = true.

Display the generated password ONCE to the admin with a Copy button.

Never store plaintext.

------------------------------------------------

FIRST LOGIN

After successful login:

If

mustChangePassword == true

Redirect immediately to

/change-password

The dashboard must NOT be accessible until password is changed.

------------------------------------------------

CHANGE PASSWORD

Create a Change Password page.

Require:

Current temporary password

New password

Confirm password

Password requirements:

Minimum 8 characters

At least one uppercase

At least one lowercase

At least one number

After successful change:

Hash password.

Update passwordHash.

Set

mustChangePassword = false

Update passwordChangedAt.

Issue a fresh JWT if necessary.

Redirect to dashboard.

------------------------------------------------

RESET PASSWORD

Only

Super Admin

or

Pharmacy Admin (for users inside their own pharmacy)

can reset passwords.

Reset generates a NEW temporary password.

Hash it.

Set

mustChangePassword = true

Reset

failedLoginAttempts

Display temporary password ONCE.

------------------------------------------------

SECURITY

Implement:

Account lock

5 failed attempts

15 minute lock

Clear error messages.

Never reveal whether the phone number exists.

Always return generic authentication errors.

------------------------------------------------

JWT

Keep the existing JWT implementation.

Reduce expiry from

90 days

to

30 days.

When expired:

Automatically clear localStorage.

Redirect to login.

------------------------------------------------

OFFLINE

Maintain all existing offline functionality.

If user is already authenticated:

Application continues functioning offline exactly as today.

Do NOT require internet after login.

However,

First login on a new device still requires internet.

Do NOT implement offline password verification.

------------------------------------------------

AUDIT LOGGING

Record:

User Created

Password Reset

Password Changed

Last Login

Store who performed administrative actions where applicable.

------------------------------------------------

ROLE PERMISSIONS

Preserve all existing permissions.

Super Admin:

Manage pharmacies

Manage admins

Manage users

Admin:

Manage pharmacists only inside assigned pharmacy

Cannot manage other pharmacies

Pharmacist:

Cannot access admin pages

Can only perform pharmacy operations already implemented

------------------------------------------------

QUALITY

Do not introduce breaking changes.

Maintain current coding style.

Maintain current folder structure.

Reuse existing helper functions wherever possible.

Avoid duplication.

Ensure TypeScript passes.

Ensure Prisma migration succeeds.

Ensure all authentication flows are tested.

At the end, provide a summary of every file modified and why.